[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "brunns-row"
version = "2.1.1"
description = "Convenience wrapper for DB API and csv.DictReader rows, and similar."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Simon Brunning", email = "simon@brunningonline.net"}
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: Implementation :: PyPy",
    "Topic :: Software Development :: Testing",
]
dependencies = []

[project.urls]
Homepage = "https://brunns-row.readthedocs.io"
Documentation = "https://brunns-row.readthedocs.io"
Repository = "https://github.com/brunns/brunns-row"
Issues = "https://github.com/brunns/brunns-row/issues"

[project.optional-dependencies]
test = [
    "pytest>=9.0",
    "pyhamcrest>=2.0",
    "contexttimer>=0.3",
    "pytest-cov>=2.5",
]
lint = [
    "ruff",
    "bandit~=1.4",
    "mypy~=1.0",
]
docs = [
    "sphinx>=3.0",
    "sphinx-autodoc-typehints>=1.10",
    "furo",
]
dev = [
    "brunns-row[test,lint,docs]",
]

[tool.hatch.build.targets.wheel]
packages = ["src/brunns"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["src"]
omit = ["*/matcher.py"]

[tool.coverage.report]
fail_under = 100
show_missing = true

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["ALL"]
ignore = ["ANN", "D", "N813", "TD", "FIX002", "FA100", "PLR0913", "COM812", "ISC001", "A005", "FBT001", "FBT002"]
exclude = ["docs/"]

[tool.ruff.lint.mccabe]
max-complexity = 5

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "N802", "PT003"]
"docs/*" = ["ERA001", "A001", "PTH", "SIM115", "INP001"]
